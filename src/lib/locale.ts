/**
 * Importing npm packages
 */

/**
 * Importing user defined packages
 */

/**
 * Defining types
 */

/**
 * Declaring the constants
 */

/**
 * The locale used for number and date formatting when a component isn't given an explicit `locale`.
 * Pinned to a stable value so the server and the client render identical text: an omitted locale falls
 * back to the runtime default — the server's ICU locale during SSR, the user's locale in the browser —
 * which produces different output on each side and breaks hydration. Pass a `locale` prop to localize.
 */
export const DEFAULT_LOCALE = 'en-US';
